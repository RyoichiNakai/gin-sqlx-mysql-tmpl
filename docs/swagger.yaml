basePath: /v1
host: localhost:8080
info:
  contact: {}
  description: 仕様書に関する内容説明
  termsOfService: 仕様書使用する際の注意事項
  title: sample
  version: "1.0"
paths:
  /example/helloworld:
    get:
      consumes:
      - application/json
      description: do ping
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping example
      tags:
      - example
swagger: "2.0"
